__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/
.env
.venv/
venv/
ENV/
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ===== 运行时数据文件（含本地配置与缓存，禁止进仓库）=====
# 注意：config.json 内含 DeepSeek API Key / Server酱 SendKey，泄露即等于密钥外泄
config.json
watchlist.json
dynamic_pool.json
notify_log.json
band_manual_list.json
kline_cache.json
kline_cache.json.tmp

# ===== 波段记忆（隐私，绝对禁止进仓库）=====
# band_memory.json 含用户手写备注（例如买入价、仓位）与入选价，
# 本仓库是 public，提交即等于公开持仓思路。
# 巡检需要的脱敏摘要另存为 band_watch.json，那一份才由 Actions 提交。
band_memory.json
band_memory.json.tmp
band_watch.json.tmp

# ===== 策略复盘（隐私，绝对禁止进仓库）=====
# band_batches.json 含批次名单与逐笔收益/胜率，属于交易绩效数据，
# 公开了等于公开自己的成绩单。**刻意不进 band_watch.json 的脱敏摘要**：
# 摘要只同步巡检必需的「代码 + 状态」，收益率永远留在本地容器。
band_batches.json
band_batches.json.tmp

# ===== 策略样本语料库（隐私 + 体积，绝对禁止进仓库）=====
# band_samples.jsonl.gz 是机器自采的全市场样本（含逐条入场特征与收益率）。
# 双重理由不进仓库：① 它是这套选股逻辑的完整"成绩单 + 特征表"，公开等于把策略送人；
# ② 几十万行、数 MB，放仓库里提交一次就再也压不下去。
# 汇总统计（band_sample_stats.json）将来若需同步，走 BAND_KEY 加密路线，不用明文。
band_samples.jsonl.gz
band_samples.jsonl.gz.tmp

# ===== 加密语料产物（可上传缓存/产物，但仍不进仓库树）=====
# band_samples.enc = 上面那份语料的 BAND_KEY 加密版，给 Actions 的缓存/产物用。
# 它**不是**明文，但也没必要占仓库体积，所以同样忽略。
band_samples.enc
band_samples.enc.tmp
# 回填用的日线磁盘缓存（几千个小文件，纯中间产物）
.sample_cache/
